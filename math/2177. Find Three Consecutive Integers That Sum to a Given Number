class Solution:
    def sumOfThree(self, num: int) -> List[int]:
        a= (num-3)/3
        if a.is_integer():
            a=int(a)
            return [a, a+1, a+2]
        else:
            return []
